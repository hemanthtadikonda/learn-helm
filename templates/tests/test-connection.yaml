#apiVersion: v1
#kind: Pod
#metadata:
#  name: "{{ include "very-simple.fullname" . }}-test-connection"
#  labels:
#    {{- include "very-simple.labels" . | nindent 4 }}
#  annotations:
#    "helm.sh/hook": test
#spec:
#  containers:
#    - name: wget
#      image: busybox
#      command: ['wget']
#      args: ['{{ include "very-simple.fullname" . }}:{{ .Values.service.port }}']
#  restartPolicy: Never
